---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../components/operator-lifecycle-manager
patches:
  # A number of operators do not work in the baseline pod-security profile, alas.
  - target:
      kind: Namespace
      name: operators
    path: patches/downgrade_pod-security.yml
