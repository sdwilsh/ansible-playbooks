---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # renovate:
  - https://github.com/cert-manager/cert-manager/releases/download/v1.11.1/cert-manager.yaml
  - clusterissuer.yml
patchesJson6902:
  - path: add_dns01_recursive_nameservers.yml
    target:
      kind: Deployment
      name: cert-manager
patchesStrategicMerge:
  - increase_replicas.yml
