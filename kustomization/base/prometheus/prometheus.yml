---
apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: prometheus
spec:
  logLevel: debug # temporary in order to debug dropped data
  ruleSelector: {}
  serviceAccountName: prometheus-service-account
  serviceMonitorSelector:
    matchLabels:
      app.kubernetes.io/name: vector
  version: v2.44.0
