---
apiVersion: v1
kind: Service
metadata:
  name: olah-svc
  labels:
    app.kubernetes.io/name: olah
spec:
  type: ClusterIP
  ports:
    - name: web
      protocol: TCP
      port: 8090
      targetPort: web
  selector:
    app.kubernetes.io/name: olah
