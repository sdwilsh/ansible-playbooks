---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-to-lldap-networkpolicy
spec:
  egress:
    - ports:
        - port: ldaps
          protocol: TCP
      to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: auth
          podSelector:
            matchLabels:
              app.kubernetes.io/name: lldap
  podSelector:
    matchLabels:
      hogs.tswn.us/lldap-client: "true"
  policyTypes:
    - Egress
