---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-cache-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: tdarr-cache-storageclass
  resources:
    requests:
      storage: 100Gi
