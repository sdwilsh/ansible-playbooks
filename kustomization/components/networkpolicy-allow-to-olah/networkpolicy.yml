---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-to-olah-networkpolicy
spec:
  egress:
    - ports:
        - port: web
          protocol: TCP
      to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: olah
          podSelector:
            matchLabels:
              app.kubernetes.io/name: olah
  podSelector:
    matchLabels:
      hogs.tswn.us/olah-client: "true"
  policyTypes:
    - Egress
