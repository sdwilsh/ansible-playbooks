---
# Soft preference (preferredDuringScheduling) toward the control plane; no-op for DaemonSets.
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - path: patches/prefer_control_plane.yml
    target:
      kind: Deployment|StatefulSet|DaemonSet
