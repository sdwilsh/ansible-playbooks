---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: jellyfin-statefulset
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: jellyfin
  serviceName: jellyfin-svc
  template:
    metadata:
      labels:
        app.kubernetes.io/name: jellyfin
    spec:
      automountServiceAccountToken: false
      containers:
        - image: jellyfin/jellyfin
          name: jellyfin
          ports:
            - containerPort: 7359
              name: discovery
              protocol: UDP
            - containerPort: 8096
              name: web
            - containerPort: 1900
              name: ssdp
          resources:
            limits:
              # A transcode needs the CPU most at the start.  It builds the
              # GPU shaders, and it reads and parses the file.  Two cores
              # make that start too slow, and the client stops before the
              # first segment is ready.
              cpu: 8000m
              memory: 8Gi
            requests:
              cpu: 500m
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            runAsNonRoot: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - mountPath: /config
              name: jellyfin-config
            - mountPath: /cache
              name: jellyfin-cache
      priorityClassName: application
      securityContext:
        fsGroup: 65534
        runAsGroup: 65534
        runAsNonRoot: true
        runAsUser: 65534
      volumes:
        - emptyDir:
            sizeLimit: 100Gi
          name: jellyfin-cache
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: jellyfin-config
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
        storageClassName: longhorn-encrypted
