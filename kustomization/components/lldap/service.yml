---
apiVersion: v1
kind: Service
metadata:
  name: lldap-svc
spec:
  ports:
    - name: ldap
      port: 389
      protocol: TCP
      targetPort: ldap
    - name: web
      port: 80
      protocol: TCP
      targetPort: web
  selector:
    app.kubernetes.io/name: lldap
