---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: una-ingress
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: una-unifi-svc
                port:
                  name: una-web
            path: /
            pathType: Prefix
