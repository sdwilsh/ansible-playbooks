---
apiVersion: v1
kind: Service
metadata:
  name: ttl-registry
  labels:
    app.kubernetes.io/name: ttl-registry
spec:
  type: ClusterIP
  ports:
    - name: web
      protocol: TCP
      port: 5000
      targetPort: 5000
  selector:
    app.kubernetes.io/name: ttl-registry
