---
apiVersion: v1
kind: Service
metadata:
  name: ttl-reaper
  labels:
    app.kubernetes.io/name: ttl-reaper
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  selector:
    app.kubernetes.io/name: ttl-reaper
