---
apiVersion: v1
kind: Service
metadata:
  name: ttl-hooks
  labels:
    app.kubernetes.io/name: ttl-hooks
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8000
      targetPort: web
  selector:
    app.kubernetes.io/name: ttl-hooks