---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authelia-podmonitor
spec:
  podMetricsEndpoints:
    - path: /metrics
      port: metrics
  selector:
    matchLabels:
      app.kubernetes.io/name: authelia
