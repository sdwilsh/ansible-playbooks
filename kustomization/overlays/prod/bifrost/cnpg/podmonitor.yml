---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: bifrost-pg-cluster-podmonitor
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: bifrost-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: bifrost-pg-cluster-ca
            key: ca.crt
        serverName: bifrost-pg-cluster-rw
