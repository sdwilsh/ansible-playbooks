---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: kube-vip-podmonitor
spec:
  podMetricsEndpoints:
    - path: /metrics
      portNumber: 2112
  selector:
    matchLabels:
      app.kubernetes.io/name: kube-vip-svc-ds
