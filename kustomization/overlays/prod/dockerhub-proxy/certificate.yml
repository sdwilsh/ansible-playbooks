---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: proxy-certificate
spec:
  secretName: proxy-tls-secret
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  commonName: dockerhub-proxy.hogs.tswn.us
  dnsNames:
    - dockerhub-proxy.hogs.tswn.us
