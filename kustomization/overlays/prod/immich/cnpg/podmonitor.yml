---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: immich-pg-cluster-podmonitor
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: immich-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: immich-pg-cluster-ca
            key: ca.crt
        serverName: immich-pg-cluster-rw
