---
kind: ConfigMap
apiVersion: v1
metadata:
  name: kubernetes-services-endpoint
  namespace: tigera-operator
data:
  # For now, reference the only control plane node to avoid kube-vip and dns.
  KUBERNETES_SERVICE_HOST: 10.11.0.101
  KUBERNETES_SERVICE_PORT: "6443"
