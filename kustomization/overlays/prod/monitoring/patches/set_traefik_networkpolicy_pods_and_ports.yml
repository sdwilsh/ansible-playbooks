---
- op: replace
  path: /spec/podSelector
  value:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values:
          - alertmanager
          - grafana
          - prometheus
- op: add
  path: /spec/ingress/0/ports/-
  value:
    port: grafana
