---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: authelia-pg-cluster
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: authelia-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: authelia-pg-cluster-ca
            key: ca.crt
        serverName: authelia-pg-cluster-rw
