---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: lldap-pg-cluster
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: lldap-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: lldap-pg-cluster-ca
            key: ca.crt
        serverName: lldap-pg-cluster-rw
