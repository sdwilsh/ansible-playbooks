---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lldap-certificate
spec:
  commonName: ldap.tswn.us
  dnsNames:
    - ldap.tswn.us
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  secretName: ldap-tswn-us-secret
