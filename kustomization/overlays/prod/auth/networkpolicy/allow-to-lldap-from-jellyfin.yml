---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-to-lldap-from-jellyfin-networkpolicy
spec:
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: aventertainment
          podSelector:
            matchLabels:
              app.kubernetes.io/name: jellyfin
      ports:
        - port: ldaps
          protocol: TCP
  podSelector:
    matchLabels:
      app.kubernetes.io/name: lldap
  policyTypes:
    - Ingress
