# Guarantee in-order delivery of QoS 1 and 2 messages.
max_inflight_messages 1

# Remove persistent client messages if we have not seen them in 90 days.
persistent_client_expiration 90d

log_type all
