---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: manyfold-pg-cluster-podmonitor
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: manyfold-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: manyfold-pg-cluster-ca
            key: ca.crt
        serverName: manyfold-pg-cluster-rw
