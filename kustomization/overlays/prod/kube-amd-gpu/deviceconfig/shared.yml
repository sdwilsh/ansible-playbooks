---
apiVersion: amd.com/v1alpha1
kind: DeviceConfig
metadata:
  name: shared
spec:
  devicePlugin:
    # No release tag exists past v1.31.0.10.  That release lacks the
    # fix for github.com/ROCm/k8s-device-plugin/issues/186.  The
    # `latest` tag includes the fix.  Pin its digest until a new
    # release ships.
    devicePluginImage: rocm/k8s-device-plugin:latest@sha256:50d0c42253ce2fedc2c89622ed0af4ebc7f1194c18a7e5b6d69e1a054c88d568
    enableDevicePlugin: true
    enableNodeLabeller: true
    # No release tag exists past v1.31.0.10.  That release lacks the
    # fix for github.com/ROCm/k8s-device-plugin/issues/186.  The
    # `labeller-latest` tag includes the fix.  Pin its digest until a
    # new release ships.
    nodeLabellerImage: rocm/k8s-device-plugin:labeller-latest@sha256:5ccb7af7d3b22621b3991730c5d805b8d1f0974d6d974bfb3430ad4db5b62628
  driver:
    # The OS image build already includes the `amdgpu` driver and
    # firmware.  The operator must not build or load the driver again.
    enable: false
  metricsExporter:
    enable: true
    prometheus:
      serviceMonitor:
        enable: true
        metricRelabelings:
          - action: labeldrop
            regex: (container|namespace|pod)
        relabelings:
          - action: replace
            sourceLabels:
              - __meta_kubernetes_pod_node_name
            targetLabel: node
    resource:
      limits:
        # This limit is intentionally tight.  This monitoring should not
        # cause memory pressure to occur!
        memory: 128Mi
      requests:
        cpu: 10m
        memory: 96Mi
  selector:
    # amdgpu-rules sets this label by PCI device ID, not by hostname.
    # The value is `shared` on any AMD GPU other than Strix Halo's
    # chip.  The value is `strixhalo` on that chip.  This keeps the
    # selector distinct from the strixhalo DeviceConfig's, on any
    # node.
    hardware.hogs.tswn.us/gpu: shared
