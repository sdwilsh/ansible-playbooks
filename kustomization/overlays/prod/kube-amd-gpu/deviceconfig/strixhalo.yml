---
apiVersion: amd.com/v1alpha1
kind: DeviceConfig
metadata:
  name: strixhalo
spec:
  devicePlugin:
    # No release tag exists past v1.31.0.10.  That release lacks the
    # fix for github.com/ROCm/k8s-device-plugin/issues/186.  The
    # `latest` tag includes the fix.  Pin its digest until a new
    # release ships.
    devicePluginImage: rocm/k8s-device-plugin:latest@sha256:1ab3351a2e36f332b8f05eef73f97e673713f25b7e83ddec0cb20cdb52631ad2
    # gpuworker01/gpuworker02 carry this taint.  Without it, the
    # device-plugin and node-labeller pods never schedule there.  The
    # operator then never advertises amd.com/gpu on these nodes.
    devicePluginTolerations:
      - effect: NoSchedule
        key: gpu
        operator: Equal
        value: strixhalo
    # No release tag exists past v1.31.0.10.  That release lacks the
    # fix for github.com/ROCm/k8s-device-plugin/issues/186.  The
    # `labeller-latest` tag includes the fix.  Pin its digest until a
    # new release ships.
    nodeLabellerImage: rocm/k8s-device-plugin:labeller-latest@sha256:5f0c9c07d76ad03e89b88349790ea34c48afbdd5603cf7c1fa875a76dee48adf
    nodeLabellerTolerations:
      - effect: NoSchedule
        key: gpu
        operator: Equal
        value: strixhalo
  driver:
    # The OS image build already includes the `amdgpu` driver and
    # firmware.  The operator must not build or load the driver again.
    enable: false
  metricsExporter:
    enable: true
    prometheus:
      serviceMonitor:
        enable: true
        interval: 15s
        metricRelabelings:
          - action: labeldrop
            regex: (container|namespace|pod)
        relabelings:
          - action: replace
            sourceLabels:
              - __meta_kubernetes_pod_node_name
            targetLabel: node
    resource:
      limits:
        # This limit is intentionally tight.  This monitoring should not
        # cause memory pressure to occur!
        # See `group_vars/kairos_gpuworker.yml`.
        memory: 128Mi
      requests:
        cpu: 10m
        memory: 96Mi
    tolerations:
      - effect: NoSchedule
        key: gpu
        operator: Equal
        value: strixhalo
  selector:
    hardware.hogs.tswn.us/gpu: strixhalo
