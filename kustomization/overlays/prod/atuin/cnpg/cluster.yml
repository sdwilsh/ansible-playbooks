---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: atuin-pg-cluster
spec:
  backup:
    target: primary
  bootstrap:
    initdb:
      database: atuin
      dataChecksums: true
      owner: atuin
      postInitApplicationSQL:
        - ALTER USER atuin SET search_path TO "$user", data, public
  env:
    - name: TZ
      value: America/Los_Angeles
  imageName: ghcr.io/cloudnative-pg/postgresql:17@sha256:11755f4e7f46b509be9c4a08d01267b50d83bfa0b6f1712c1714ea72956e2318
  instances: 3
  managed:
    services:
      disabledDefaultServices:
        - r
        - ro
  monitoring:
    tls:
      enabled: true
  plugins:
    - enabled: true
      isWALArchiver: true
      name: barman-cloud.cloudnative-pg.io
      parameters:
        barmanObjectName: garage-objectstore
  postgresql:
    synchronous:
      failoverQuorum: true
      method: any
      number: 1
  primaryUpdateMethod: switchover
  primaryUpdateStrategy: unsupervised
  priorityClassName: application-infra
  storage:
    size: 10Gi
    storageClass: cnpg-data-encrypted-storage
  walStorage:
    size: 4Gi
    storageClass: cnpg-wal-encrypted-storage
