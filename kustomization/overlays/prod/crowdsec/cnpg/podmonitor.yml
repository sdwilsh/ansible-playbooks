---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: crowdsec-pg-cluster
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: crowdsec-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: crowdsec-pg-cluster-ca
            key: ca.crt
        serverName: crowdsec-pg-cluster-rw
