---
kind: ConfigMap
apiVersion: v1
metadata:
  name: kubernetes-services-endpoint
  namespace: tigera-operator
data:
  # k3s.hogs.tswn.us, but I want to avoid the DNS lookup.
  KUBERNETES_SERVICE_HOST: 10.11.0.100
  KUBERNETES_SERVICE_PORT: "6443"
