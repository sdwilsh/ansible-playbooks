---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: tandoor-pg-cluster-podmonitor
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: tandoor-pg-cluster
  podMetricsEndpoints:
    - port: metrics
      scheme: https
      tlsConfig:
        ca:
          secret:
            name: tandoor-pg-cluster-ca
            key: ca.crt
        serverName: tandoor-pg-cluster-rw
