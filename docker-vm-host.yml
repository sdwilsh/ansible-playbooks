---
- hosts: "docker_vm"
  become: yes
  roles:
    - role: "common"
    - role: "nickjj.docker"
      tags:
        - "docker"
    - role: "docker-networks"
      tags:
        - "docker"
    - role: "zabbix"
      tags:
        - "docker"
        - "zabbix"
