.ansible_vault_password
.pnpm-store
.venv
node_modules

# Used to configure bootc images with a pre-defined user.
config.toml
output-loki
output-mack
output-mack-kairos

# `ansible-galaxy` and `ansible-lint` write Roles and Collections here.
.ansible/

# Helm chart expansion by kustomize.  This isn't the suggested way, but home-prod isn't prod.
kustomization/**/charts
