---
# This is an external-to-the-cluster service.
- name: Setup Service for minio
  kubernetes.core.k8s:
    api_version: v1
    context: "{{ cluster }}"
    kind: Service
    resource_definition:
      metadata:
        name: minio-svc
        namespace: default
      spec:
        type: ExternalName
        externalName: firstlightweaveslivingsong.hogs.tswn.us
    state: present
