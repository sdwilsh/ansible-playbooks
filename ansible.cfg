[defaults]
collections_path=external_collections:/usr/share/ansible/collections
INTERPRETER_PYTHON=auto
inventory=prod-inventory
remote_user=ansible
roles_path=roles:external_roles
VAULT_PASSWORD_FILE=.ansible_vault_password

[persistent_connection]
COMMAND_TIMEOUT=180

[ssh_connection]
scp_if_ssh=true
